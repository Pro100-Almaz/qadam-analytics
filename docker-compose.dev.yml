version: '3.8'
services:
  app:
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      DEBUG: "True"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
  db:
    ports:
      - "5432:5432"  # ok in dev only

